CREATE TABLE EMPLOYEE3(
EMPID VARCHAR(23) PRIMARY KEY CHECK(EMPID LIKE 'S%'),
EMPNAME VARCHAR(25) NOT NULL,
DOB DATE NOT NULL,
DOJ DATE  NOT NULL,
SALARY NUMBER(7,0) CHECK(SALARY>0),
CONSTRAINT CK CHECK (DOB>DOJ)
);
